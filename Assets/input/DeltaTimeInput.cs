using Unity.Cinemachine;
using UnityEngine;

namespace input
{
    public class DeltaTimeInput : CinemachineInputAxisController
    {
        // public override float GetAxisValue(int axis) => base.GetAxisValue(axis) / Time.deltaTime;
    }
}
